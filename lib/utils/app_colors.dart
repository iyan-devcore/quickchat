import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFF008069); // WhatsApp Green
  static const Color secondary = Color(0xFF25D366); // Lighter Green
  static const Color accent = Color(0xFF128C7E); // Darker Green

  // Backgrounds
  static const Color backgroundLight = Color(0xFFFFFFFF);
  static const Color backgroundDark = Color(0xFF111B21);
  static const Color chatBackgroundLight = Color(0xFFE5E5E5); // Default chat bg
  static const Color chatBackgroundDark = Color(0xFF0B141A);

  // Text
  static const Color textLight = Color(0xFF111B21);
  static const Color textDark = Color(0xFFE9EDEF);
  static const Color textGrey = Color(0xFF8696A0);

  // UI Elements
  static const Color dividerLight = Color(0xFFE9EDEF);
  static const Color dividerDark = Color(0xFF202C33);
  static const Color inputBackgroundLight = Color(0xFFFFFFFF);
  static const Color inputBackgroundDark = Color(0xFF202C33);
  
  // Status
  static const Color unreadBadge = Color(0xFF25D366);
  static const Color online = Color(0xFF25D366);
}
